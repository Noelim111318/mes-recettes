# icons/

Placeholders unis (fond sombre + carre jaune) pour que l'app soit servable
tout de suite, avant d'avoir Pillow.

Les vraies icones :

```
python3 tools/make-icons.py
```

Ecrase ces fichiers + genere `../favicon.ico` et `icon-512-maskable.png`
avec la bonne marge de securite.
